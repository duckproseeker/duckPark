These sample descriptors target the retired builtin/native scenario path.

The active scenario catalog now comes from `app/scenario/official_runner.py`,
so `configs/scenarios/` is intentionally left out of the main flow to avoid
misleading new changes.
