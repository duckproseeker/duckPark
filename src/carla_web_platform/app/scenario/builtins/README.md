Builtin scenario modules were retired from the active product path.

Active scenarios now come from the official ScenarioRunner catalog in
`app/scenario/official_runner.py`. The original implementations live under
`app/deprecated/native_runtime/scenario/builtins/`.
